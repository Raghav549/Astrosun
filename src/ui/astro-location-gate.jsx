import React,{useEffect,useRef,useState}from'react';
import{MapPin,ShieldCheck,Navigation,LoaderCircle}from'lucide-react';

export function AstroLocationGate({onAllowed,children}){const[status,setStatus]=useState('checking');const requested=useRef(false);useEffect(()=>{if(requested.current)return;requested.current=true;const supported=typeof navigator!=='undefined'&&'geolocation'in navigator;if(!supported){setStatus('unsupported');return}setStatus('requesting');navigator.geolocation.getCurrentPosition(pos=>{sessionStorage.setItem('astrosun-location-approved','1');sessionStorage.setItem('astrosun-location-lat',String(pos.coords.latitude));sessionStorage.setItem('astrosun-location-lon',String(pos.coords.longitude));setStatus('allowed');onAllowed?.(pos)},()=>setStatus('denied'),{enableHighAccuracy:true,timeout:10000,maximumAge:300000})},[onAllowed]);
if(status==='checking'||status==='requesting')return <div className="location-gate"><div className="location-gate-card"><div className="location-gate-icon"><LoaderCircle className="spin"/></div><span>ASTROSUN · LOCATION</span><h1>स्थान की अनुमति माँगी जा रही है</h1><p>सटीक आकाश, सूर्योदय-सूर्यास्त, क्षितिज और स्थानीय पंचांग के लिए आपके डिवाइस का वर्तमान स्थान चाहिए।</p><small>ब्राउज़र अनुमति संवाद अभी ऊपर दिख सकता है।</small></div></div>;
if(status==='denied'||status==='unsupported')return <div className="location-gate"><div className="location-gate-card"><div className="location-gate-icon"><MapPin/></div><span>ASTROSUN · LOCATION</span><h1>स्थान अनुमति आवश्यक है</h1><p>AstroSun बिना स्थान के सामान्य mode में चल सकता है, लेकिन स्थानीय आकाश-निरीक्षण और location-based calculations अधूरी रहेंगी।</p><button onClick={()=>window.location.reload()} className="location-gate-primary"><Navigation size={17}/> फिर अनुमति माँगें</button><button onClick={()=>{sessionStorage.setItem('astrosun-location-skipped','1');onAllowed?.(null)}} className="location-gate-secondary">बिना स्थान जारी रखें</button></div></div>;
return children}
